"use client";

const AddOrder = () => {
  const handleMenuOrder = () => {};

  return (
    <div>
      <button onClick={handleMenuOrder} className=" p-2 border border-gray-400">
        Add order
      </button>
    </div>
  );
};

export default AddOrder;
